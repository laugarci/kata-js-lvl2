
//Escribe una funcion que sume los dos numeros que llegan por parametro y retorne true si es el resultado es igual o mayor que 10
function sumAndReturn(num1, num2) {

}

//Escribe una funcion que retorne true si el numero que llega es multiple de 5 y false en caso contrario
function multipleFive(num) {
	//escribe el codigo aqui
}

//Escribe una funcion que devuelva true si el parametro que llega es numero y false si no lo es
function isNum(num) {
	//escribe el codigo aqui

}

//Escribe una funcion que una las dos cadenas que llegan y devuelva la largada de ambas juntas
function twoArraysLen (str1, str2) {
	//escribe el codigo aqui
}

//Escribe una funcion que use el metodo adecuado para ver cual de los dos numeros es mayor y retorne la resta del mayor y el menor
function calculate(num1, num2) {
	//escribe el codigo aqui
}

//Escribe una funcion que corte el string que le llega por parametros y devuelva la primera palabra
function cutAndReturn(str) {
	//escribe el codigo aqui

}

//Usa el metodo adecuado para ver si "element" esta dentro del array. Retorna true si si y false si no
function elementinArray(array, element) {
	//escribe el codigo aqui
}

//SIN USAR .length haz una funcion que calcule el tamano de un array
function calculeLen(array) {
	//escribe el codigo aqui

}

//Escribe una funcion que devuelva true si el numero que recibe es positivo y false si es negativo
function isNegOrPos(array) {
	//escribe el codigo aqui
}

//Escribe una funcion que mire si el nombre que llega por parametro esta dentro del array "arr". Si lo esta, retorna false, si no lo esta, crea una nueva cadena, anade el nombre nuevo y retorna esa cadena
function isInside(name) {
	const arr = ["Laura", "Pepa", "Manuel"];
	//escribe el codigo aqui
}

//Escribe una funcion que recibe un array y devuelve un nuevo array sin elementos duplicados
function removeDuplicates(array) {

}

//Escribe una funcion que recibe una string y la devuelva invertida
function reverseString(str) {

}

module.exports = {
	sumAndReturn,
	multipleFive,
	isNum,
	twoArraysLen,
	calculate,
	cutAndReturn,
	elementinArray,
	calculeLen,
	isNegOrPos,
	isInside,
	removeDuplicates,
	reverseString,
}
